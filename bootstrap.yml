spring:
  application:
    name: s-toKafka-service
  profiles:
    active: s_to_kafka
  cloud:
    config:
      name: s-toKafka-service, config-client,config-service
      uri: htpp:/localhost:8888/
